<div class="panel top-list">
  <div class="panel-heading">
    <h4>Popular Links</h4>
  </div>
  <div class="panel-body">
    <ul class="list-group">
    <% @popular_links.each do |item| %>
    <li class="presentation list-group-item">
      <span class="badge badge-warning">
        <%= pluralize(item[1], "visit") %>
      </span>
      <%= link_to Link.find(item[0]).shortened_url, 
          Link.find(item[0]).shortened_url, target: '_blank' %>
    </li>
    <% end %>
    </ul>
  </div>
</div>
    
    
